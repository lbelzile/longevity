# Check that simulated values are within
# lower and upper bounds and
# fail with invalid parameter values

library(longevity)
library(tinytest)

# Check that simulated values are within bounds

# Check we get warning with invalid parameters

# Check that boundary parameters get mapped
